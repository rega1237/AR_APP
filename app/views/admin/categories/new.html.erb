<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New Category for <%= @menu.name %></h1>
  
  <%= render "form", category: @category %>

  <%= link_to "Back to Menu", admin_menu_path(@menu), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
