<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New Dish for <%= @category.name %></h1>
  
  <%= render "form", dish: @dish %>

  <%= link_to "Back to Menu", admin_menu_path(@category.menu), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
